# Make the "engines" requirement in package.json a hard error instead of a
# silent warning — catches "wrong Node version" (e.g. node:sqlite missing on
# Node < 22.5) at `npm install`/`npm run dev` time with a clear message,
# instead of a cryptic crash deep in Next.js's instrumentation hook.
engine-strict=true
